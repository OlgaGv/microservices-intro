apiVersion: v1
kind: Service
metadata:
  name: resources-db
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: resources-db
  ports:
    - port: 5432
      targetPort: 5432
