apiVersion: v1
kind: Service
metadata:
  name: resources-ms
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: resources-ms
  ports:
    - port: 8080
      targetPort: 8080
